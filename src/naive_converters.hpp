#ifndef NAIVE_CONVERTERS_HPP
#define NAIVE_CONVERTERS_HPP

#include<pagmo/population.hpp>

// Naive converters
pagmo::population test_population_converters(pagmo::population pop)
{
    return pop;
}

#endif